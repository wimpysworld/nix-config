diff --git a/tools/environments/local.py b/tools/environments/local.py
index 2631a65..487bcf1 100644
--- a/tools/environments/local.py
+++ b/tools/environments/local.py
@@ -217,13 +217,20 @@ class LocalEnvironment(BaseEnvironment):
 
     Spawn-per-call: every execute() spawns a fresh bash process.
-    Session snapshot preserves env vars across calls.
+    Optional session snapshot preserves env vars across calls when enabled.
     CWD persists via file-based read after each command.
     """
 
-    def __init__(self, cwd: str = "", timeout: int = 60, env: dict = None):
+    def __init__(
+        self,
+        cwd: str = "",
+        timeout: int = 60,
+        env: dict = None,
+        persistent: bool = False,
+    ):
         super().__init__(cwd=cwd or os.getcwd(), timeout=timeout, env=env)
-        self.init_session()
+        if persistent:
+            self.init_session()
 
     def get_temp_dir(self) -> str:
         """Return a shell-safe writable temp dir for local execution.
diff --git a/tools/terminal_tool.py b/tools/terminal_tool.py
index 32b07b8..1c9f9ea 100644
--- a/tools/terminal_tool.py
+++ b/tools/terminal_tool.py
@@ -713,7 +713,12 @@ def _create_environment(env_type: str, image: str, cwd: str, timeout: int,
     docker_forward_env = cc.get("docker_forward_env", [])
     docker_env = cc.get("docker_env", {})
 
     if env_type == "local":
-        return _LocalEnvironment(cwd=cwd, timeout=timeout)
+        lc = local_config or {}
+        return _LocalEnvironment(
+            cwd=cwd,
+            timeout=timeout,
+            persistent=lc.get("persistent", False),
+        )
 
     elif env_type == "docker":
         return _DockerEnvironment(
