## Create AGENTS.md

Load the `write-agents-md` skill and run its **create** flow.

Target path argument: $1. Use it if provided; otherwise default to `AGENTS.md` at the repo root, or ask if the scope is unclear.

Apply `write-agents-md` end-to-end: discovery, canonical sections, content rules, sizing, output shape. Do not duplicate that guidance here.
