## Update AGENTS.md

Load the `write-agents-md` skill and run its **update** or **consolidate** flow on the target.

Target file argument: $1. If blank, discover instruction files using the consolidation list in `write-agents-md/references/platforms.md`.

Apply `write-agents-md`: diagnose, flag conflicts before editing, preserve runnable commands, emit the changed file plus the changelog. Do not duplicate that guidance here.
