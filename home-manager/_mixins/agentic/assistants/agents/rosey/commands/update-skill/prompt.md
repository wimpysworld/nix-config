## Update Skill

Load the `write-skill` skill and run its **update** flow on the target.

Skill path argument: $1. If blank, ask for the skill directory or `SKILL.md` path.

Apply `write-skill`: diagnose description, body, structure, and bundled resources; preserve `name` and directory; emit the changed files plus a short changelog. Do not duplicate that guidance here.
