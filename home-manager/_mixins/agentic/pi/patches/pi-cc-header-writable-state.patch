--- a/extensions/pi-cc-header.ts
+++ b/extensions/pi-cc-header.ts
@@ -526 +526 @@
-	const settingsPath = join(
+	const statePath = join(
@@ -530 +530,2 @@
-		"settings.json",
+		"state",
+		"pi-cc-header.json",
@@ -534 +535 @@
-			return JSON.parse(readFileSync(settingsPath, "utf-8"));
+			return JSON.parse(readFileSync(statePath, "utf-8"));
@@ -540 +541 @@
-		writeFileSync(settingsPath, JSON.stringify(s, null, 2) + "\n", "utf-8");
+		writeFileSync(statePath, JSON.stringify(s, null, 2) + "\n", "utf-8");
@@ -552,10 +553 @@
-			if (s.rsl !== false) {
-				s.quietStartup = true;
-				s.clearOnStart = true;
-				saveSettings(s);
-				process.stdout.write("\x1b[2J\x1b[3J\x1b[H");
-			} else {
-				s.quietStartup = false;
-				s.clearOnStart = false;
-				saveSettings(s);
-			}
+			process.stdout.write("\x1b[2J\x1b[3J\x1b[H");
@@ -575,2 +566,0 @@
-				s.quietStartup = true;
-				s.clearOnStart = true;
@@ -585,2 +574,0 @@
-				s.quietStartup = false;
-				s.clearOnStart = false;
@@ -591,3 +578,0 @@
-				ctx.ui.setFooter(undefined);
-				ctx.ui.setWorkingIndicator();
-				ctx.ui.setEditorComponent(undefined);
@@ -735 +720 @@
-		description: "Toggle resource list visibility (applies on next session)",
+		description: "Explain resource list configuration",
@@ -737,7 +721,0 @@
-			const s = getSettings();
-			if ((s.ccHeader || {}).disabled) {
-				ctx.ui.notify("pi-cc-header is disabled, use /htg to enable", "info");
-				return;
-			}
-			s.rsl = s.rsl === false ? true : false;
-			saveSettings(s);
@@ -745 +723 @@
-				`Resource list: ${s.rsl !== false ? "HIDDEN" : "VISIBLE"}`,
+				"Home Manager controls the resource list through quietStartup and clearOnStart",
@@ -748 +725,0 @@
-			setTimeout(() => ctx.reload(), 100);
