{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Noughty user registry",
  "description": "User registry for noughty. Each key is a username.",
  "type": "object",
  "additionalProperties": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "tags": {
        "type": "array",
        "items": { "type": "string" }
      }
    }
  }
}
