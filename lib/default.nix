{
  darwinStateVersion,
  inputs,
  outputs,
  stateVersion,
  ...
}:
let
  helpers = import ./helpers.nix {
    inherit
      darwinStateVersion
      inputs
      outputs
      stateVersion
      ;
  };
in
{
  inherit (helpers)
    mkDarwin
    mkHome
    mkNixos
    forAllSystems
    mkSystemConfig
    generateConfigs
    isLinuxEntry
    isDarwinEntry
    isISOEntry
    isHomeOnlyEntry
    ;
}
