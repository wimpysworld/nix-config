id = "local.work-layout"
name = "Work layout"
version = "0.2.0"
min_herdr_version = "0.8.2"
description = "Prepare shell tabs for new workspaces"
platforms = ["linux"]

[[events]]
on = "workspace.created"
command = ["bin/herdr-work-layout"]
